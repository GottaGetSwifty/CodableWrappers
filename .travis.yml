matrix:
  include:
    - os: osx
      osx_image: xcode12
      env: SWIFT_VERSION=5.2
language: generic
sudo: required
dist: trusty
script:
  - swift test --enable-code-coverage
after_success:
  - bash < (curl -s https://codecov.io/bash)